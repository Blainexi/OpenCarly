{
  "version": "0.1",
  "global": { "always": ["preferences"] },
  "domains": {
    "development": {
      "triggers": ["fix", "bug", "build", "refactor", "code"],
      "rules": ["RULE_01_development_core"]
    },
    "brief": {
      "triggers": ["*brief"],
      "rules": ["RULE_brief_mode"]
    }
  }
}