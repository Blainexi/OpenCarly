{
  "name": "opencarly",
  "version": "0.1.0",
  "description": "CARLY - Just-in-time rules for Opencode. Rules load only when relevant.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.0"
  },
  "peerDependencies": {
    "opencode": ">=1.2.0"
  },
  "keywords": ["opencode", "plugin", "carly", "context", "rules"],
  "author": "Blaine + Grok 4.2",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Blainexi/OpenCarly"
  }
}